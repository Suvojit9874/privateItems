spring:
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: update
#      create, create-drop, validate, and update
    show-sql: false
    properties:
      hibernate:
        format_sql: false
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect